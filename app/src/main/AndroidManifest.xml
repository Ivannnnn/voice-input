<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@style/Theme.CloudVoiceInput">

        <!-- Normal launcher activity, useful for testing/settings later. -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          This is the important part.
          HeliBoard can invoke an app that handles ACTION_RECOGNIZE_SPEECH.
        -->
        <activity
            android:name=".RecognizerActivity"
            android:exported="true"
            android:theme="@style/Theme.CloudVoiceInput.Dialog">
            <intent-filter>
                <action android:name="android.speech.action.RECOGNIZE_SPEECH" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

    </application>
</manifest>
